import React from "react";

const Section = ({ children }) => {
  return (
    <section
      style={{
        margin: "0px 10% 44px 10%"
      }}
    >
      {children}
    </section>
  );
};

export default Section;
